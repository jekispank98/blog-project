[package]
name = "blog-client"
version = "0.1.0"
edition = "2021"

[lib]
name = "blog_client"
path = "src/lib.rs"

[dependencies]
tonic = "0.12"
tokio = { version = "1", features = ["full"] }
prost = "0.14"

[build-dependencies]
tonic-build = "0.12"
